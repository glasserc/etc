db/oplog.*
db/makefile.local
db/nohup.out
*~
# emacs lock files
.#*
# emacs autosaves -- this might not match all of them
*#
# direnv files.
# .envrc describes what environment variables to load.
.envrc
# .direnv is where certain local environments are set up using 'layout python' etc.
.direnv
