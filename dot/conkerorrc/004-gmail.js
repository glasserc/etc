// FIXME: you can use C-s to search to a link, but opening it doesn't
// seem obvious..

// Escape unfocuses links, which is occasionally useful.
// Unfortunately, gmail also uses it to unfocus its own widgets..
// FIXME: call unfocus and then fall through?
//define_key(gmail_keymap, "escape", null, $fallthrough);
